# Christopher Iliffe Sprague
# sprague@kth.se

class Obstacle(object):

    def __init__(self, x, y, lr, ur):

        # cartesian position
        self.x = float(x)
        self.y = float(y)

        #
